# Database Configuration
# PostgreSQL connection string
# Format: postgresql://user:password@host:port/database?sslmode=require
DATABASE_URL=postgresql://user:password@host:port/database?sslmode=require

# API Configuration
# API key for authenticating requests from the frontend
# This should match the API_KEY in your frontend .env.local file
API_KEY=your-api-key-here

# Optional: Base URL for the API
# Default: http://localhost:8000
BASE_URL=http://localhost:8000

# Optional: Endpoint URL for external API calls
# Default: /profile/search
ENDPOINT_URL=/profile/search
